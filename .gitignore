# OS & logs
.DS_Store
*.log
*.local
.env

# Build outputs
dist-ssr
server/dist
public/dist
.turbo
.cache

# Node
node_modules

# Yarn Berry (Zero-Install)
# 기본적으로 .yarn 아래는 무시하되, zero-install에 필요한 것들은 예외 처리해 커밋합니다.
.yarn/*
!.yarn/cache
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
!.yarn/install-state.gz   # 브랜치 전환 시 재설치 방지에 도움

# PnP 런타임 파일은 커밋 (zero-install에 필요)
!.pnp.cjs
!.pnp.loader.mjs

# 생성되더라도 굳이 커밋할 필요 없는 PnP 바이너리 데이터
.pnp.data.mdb